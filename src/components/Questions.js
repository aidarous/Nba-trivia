import React, { useState} from 'react';

const Questions = ({id, question}) => {
    return (
        <div>
            <h4>{question}</h4>
        </div>
    );
};

export default Questions;